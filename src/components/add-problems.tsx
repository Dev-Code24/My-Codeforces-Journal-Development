import Button from "../ui/button";

const AddProblems = () => {
  return (
    <div className="flex items-center justify-center h-full">
      <Button>Add this problem</Button>
    </div>
  );
};
export default AddProblems;
